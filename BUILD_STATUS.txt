Loop Roll build status

The source now contains the corrected skin controls and beat-grid alignment.
GitHub Actions builds the x64 DLL using the official VirtualDJ SDK headers.
The checked-in baseline DLL is retained for comparison only and is not rebuilt
by this source change.
